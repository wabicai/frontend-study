```js
    var h1 = pHead1;
    var h2 = pHead2;

    while(h1 !== h2){ // 如果相交、或者没有相交
        h1 = h1 === null ? pHead2: h1.next; // h1结束 接入对方
        h2 = h2 === null ? pHead1: h2.next;  // h2结束 接入对方
    }

    return h1;
```

