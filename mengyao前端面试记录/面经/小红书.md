JD
社区
工作职责：
1. 负责小红书社区基础产品和创作者服务的业务开发，包括社区话题、创作中心/创作服务平台、微信小程序等
2. 参与社区 S 级运营活动开发，拓宽用户对于社区内容的多品类心智，提升用户认知和参与度
3. 关注线上产品的体验和质量，优化产品性能和交互，为用户和创作者带来更好的使用和创作体验
任职资格：
1. 本科以上学历，两年以上前端开发经验，有移动端或跨平台应用开发经验
2. 熟练掌握前端领域相关技术，包括但不仅限于 JS/TS、Node.js 等
3. 熟练掌握 Vue / React / React Native 前端 UI 框架，了解框架基本原理
4. 了解跨端应用性能优化方案并有成熟思路，能够建立较好的业务性能指标并分析和优化性能数据
5. 乐观细心，具备良好的跨团队协作和沟通能力，作为业务 BP（Business Partner）积极推动项目进展


/*
 * 实现函数 fn, fn 接受如下数据结构的参数
 * fn 的返回值为数组，数组项为含有 children 的元素的 id
 * 例如如下数据结构的返回为：['1', '11', '112', '12']
 */
const cityData = [{
  id: '1',
  name: '广东省',
  children: [
    {
      id: '11',
      name: '深圳市',
      children: [
        {
          id: '111',
          name: '南山区'
        },
        {
          id: '112',
          name: '福田区',
          children: [{
            id: '1121',
            name: 'A街道'
          }]
        }
      ]
    },
    {
      id: '12',
      name: '东莞市',
      children: [
        {
          id: '121',
          name: 'A区'
        },
        {
          id: '122',
          name: 'B区',
        }
      ]
    }
  ]
}];

function fn (list) {
	let result = [];
	process(list, result);
	return result;
}

function process(list, result){
	for(let i = 0;i<list.length;i++){
		let item = list[i];
		result.push(item.id);
		if(item.children && item.children.length){
			process(item.children);
		}
	}
}

console.log(fn(cityData))

1.三个项目感觉差不多，能说说吗
1.做项目中的困难和挑战
2.新白板中有多少人开发，扮演什么角色
3.面向对象+事件驱动，讲讲；碰撞的时候，重叠的元素怎么处理的
4.录屏的实现，实际是录播课
5.浏览器垃圾回收机制
6.小程序环境和浏览器环境的区别
7.小程序双线程
8.白板UI层为什么使用VUE2不用VUE3
9.浏览器收到html文档后，到渲染的过程
10.外链CSS加载是否会阻塞
	1.css加载不会阻塞DOM树的解析
	2.css加载会阻塞DOM树的渲染
	3.css加载会阻塞后面js语句的执行
	* 从渲染过程来分析
	1.DOM解析和CSS解析是两个并行的进程，所以这也解释了为什么CSS加载不会阻塞DOM的解析。
	2.然而，由于Render Tree是依赖于DOM Tree和CSSOM Tree的，所以他必须等待到CSSOM Tree构建完成，也就是CSS资源加载完成(或者CSS资源加载失败)后，才能开始渲染。因此，CSS加载是会阻塞Dom的渲染的。
	3.由于js可能会操作之前的Dom节点和css样式，因此浏览器会维持html中css和js的顺序。因此，样式表会在后面的js执行前先加载执行完毕。所以css会阻塞后面js的执行
	* 尽可能的提高css加载速度，比如可以使用以下几种方法:
	1.使用CDN(因为CDN会根据你的网络状况，替你挑选最近的一个具有缓存内容的节点为你提供资源，因此可以减少加载时间)
	2.对css进行压缩(可以用很多打包工具，比如webpack,gulp等，也可以通过开启gzip压缩)
	3.合理的使用缓存(设置cache-control,expires,以及E-tag都是不错的，不过要注意一个问题，就是文件更新后，你要避免缓存而带来的影响。其中一个解决防范是在文件名字后面加一个版本号)
	4.减少http请求数，将多个css文件合并，或者是干脆直接写成内联样式(内联样式的一个缺点就是不能缓存)
11.BFC
12.Vue数组变化，监听；Vue2 Vue3数据监听差异
13.Vue 2?3?中更新阶段的声明周期 render?
14.nextick
15.webpack babel vite 介绍babel用来做什么的，有哪些方法实现，原理
16.babel transform run time
17.webpack和vite的区别
18.Vue3的了解
19.看代码输出
(1)构造函数中的this，修改箭头函数，修改构造函数的return
(2)微任务宏任务，resolve，resolve后还执行吗
20.写函数，树形遍历出来每个元素ID
21.怎么学习前端的

问题：
介绍：团队规模2-30人，团队负责内容 社区前端，很多，技术栈Vue3.0 ts；

二面
自我介绍 艾德 负责社区前端
1.自我介绍
2.介绍个人经历，角色的变化
3.介绍团队，需求分工
4.三个项目有什么关联
5.互动课堂做了什么
✅6.小程序兼容sdk做了什么，有什么功能的变动，细节
✅7.小程序打包，其他打包工具的按需引入的原理
8.稳定性
✅9.白板的优化逻辑是什么意思，具体优化了哪里
10.性能优化的衡量；如何定义一套这样的衡量指标；
✅如果要实现一种衡量FPS的指标，怎么做，能够不影响主逻辑
✅11.canvas实现中最复杂的一个场景，复杂在哪里，技术难点是什么 （应该答svg绘制图形）
✅12.最大的收获是什么、
✅13.白板处于什么阶段，为什么和旧白板有冲突
✅14.ts具体在SDK里有什么实现，配置是严格配置的吗
✅15.题目
写了十分钟
/**
 * 验证括号是否成功匹配
 * @description 给定一个只包含 '('，')'，'['，']'，'{'，'}' 的字符串（支持嵌套），判断是否按相同类型的括号和顺序进行闭合
 * @param {String} input 原始字符串
 * @returns {Boolean} 返回是否符合匹配规则
 *
 * @example
 * solution('()')     // true
 * solution('()[]{}') // true
 * solution('([{}])') // true
 * solution('(]')     // false
 * solution('([)]')   // false
 */
```js
function solution(arr) {
	let stack = [];
	let map = new Map([['(', ')'], ['[', ']'], ['{', '}']]);
	
	for(let i = 0; i < arr.length; i++){
		if(map.has(arr[i])) stack.push(arr[i]);
		else {
			if(stack.length === 0 || arr[i] !== map.get(stack.pop())) 
				return false
		}
	}
	return true;
}

console.log(solution('()'))     // true
console.log(solution('()[]{}')) // true
console.log(solution('([{}])')) // true
console.log(solution('(]'))     // false
console.log(solution('([)]'))   // false
```
16
对产品的理解，深度用户吗
17.开放性问题不记得了，网不好去客厅了
18.我提问
技术栈 vue3 RN 小程序
技术场景
北京前端7 8个人，ugc闭环的
![[Pasted image 20220904163214.png]]

✅5.屏幕分享、录制回放怎么做的，有什么技术缺陷，流是什么？难点；稳定性、延时怎么保证；心跳
6.教师培训平台
7.白板：优化逻辑优化了哪里，从技术角度说重构有什么好处；性能优化的衡量；如何定义一套这样的衡量指标