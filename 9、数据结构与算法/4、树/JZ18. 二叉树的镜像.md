```js
function Mirror( pRoot ) {
    if(!pRoot) return null
    let p = pRoot
    // write code here
    if(p.left||p.right){
       let temp = p.left
       p.left = p.right
        p.right = temp
    }

    if(pRoot.left){
            Mirror(pRoot.left)
    }
    if(pRoot.right){
            Mirror(pRoot.right)
    }
    return pRoot
}
module.exports = {
    Mirror : Mirror
};
```

