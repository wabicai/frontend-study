```js
/**
 * 反转字符串
 * @param str string字符串 
 * @return string字符串
 */
function solve( str ) {
    // write code here
//     str = str.split("")
//     str = str.reverse()
//     str = str.join("")
//     return str
     let newStr = '';
    for(let i = str.length - 1; i >= 0; i--){
        newStr += (str[i]);
    }
    return newStr;
    
    let arr=[];
    for(let i of str){
        arr.unshift(i)
    }
    return arr.join('')
    
    return Array.from(str).reverse().join('')
}
module.exports = {
    solve : solve
};
```

