```js
function Fibonacci(n)
{
    // write code here
    let stack = [0,1]
    if(n == 0 || n == 1) return stack[n]

    for(let i = 2;i < n+1;i++){
        stack[i] = stack[i-1] + stack[i-2]
    }
    return stack[n]
}
module.exports = {
    Fibonacci : Fibonacci
};
```

